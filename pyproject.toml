[project]
name = "epi-with-keypoints"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.10"
dependencies = [
    "rfdetr[loggers,train]>=1.8.3",
    "torch>=2.13.0",
]
